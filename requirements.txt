# Python deps for pick_and_place_voice, pinned to what the working machine runs.
# Install with:  pip3 install -r requirements.txt
#
# Two pins are load-bearing - do not "upgrade" them:
#
#   pymodbus 2.5.3 : robot_control/onrobot.py imports `pymodbus.client.sync`,
#                    which was REMOVED in pymodbus 3.x. On 3.x the gripper
#                    fails to connect and the arm runs with no grip.
#   numpy 1.24.4   : numpy 2.x breaks the installed torch/ultralytics build.

# --- vision ---
ultralytics==8.4.112
opencv-python==4.10.0.84
numpy==1.24.4
scipy==1.15.3

# --- gripper (see warning above) ---
pymodbus==2.5.3

# --- voice path only (wake word -> STT -> keyword). The keyboard-driven
#     tracking (F / G) works without any of these. ---
openwakeword==0.6.0
tflite-runtime==2.14.0
sounddevice==0.5.5
PyAudio==0.2.14
openai==1.98.0
langchain==0.3.27
langchain-openai==0.3.28
langchain-core==0.3.86
python-dotenv==1.2.2

# --- build-time: colcon's ament_python build imports setuptools_scm, which
#     needs vcs_versioning. Without it EVERY ament_python package fails to
#     build with "ModuleNotFoundError: No module named 'vcs_versioning'". ---
setuptools==75.6.0
setuptools-scm==10.2.1
vcs-versioning==2.2.3
