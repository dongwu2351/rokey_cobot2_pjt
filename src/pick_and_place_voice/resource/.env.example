# Copy to `.env` in this same directory and fill in a real key.
# Only needed for the voice path (get_keyword uses Whisper + GPT-4o);
# keyboard-driven tracking (F / G) runs without it.
OPENAI_API_KEY="sk-..."
